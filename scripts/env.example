# Copy to .env at the repo root and fill in your key.
# Get a key at https://console.cloud.google.com/ → APIs & Services → Credentials
# Enable the "Places API (New)" before using it.
GOOGLE_PLACES_API_KEY=your-api-key-here
